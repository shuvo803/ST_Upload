# Secrets — never commit these, set them as Railway variables instead
.env
cookies.txt

# Runtime/download folders created while the bot runs
downloads/
Metadata/

# Python
__pycache__/
*.pyc
.venv/
venv/

# OS/editor noise
.DS_Store
.vscode/
